1.python2中的if语句的判断条件要用括号括起来,python3不用。
2.int,str,float,tuple为不可变对象。可变对象有dict,list。
3.str的改变方法有，1)用replace方法，2)将字符串转换为列表然后用join连接，3)用切片
4.enumerate返回的是元素的索引和z值构成的一个一个元组
5.zip是将对象对应的元素打包成一个一个元组
6.一个列表乘于负数时，返回为空
7.函数式编程的一个特点就是允许函数把本身作为参数传入另一个函数，还允许返回一个函数
8.用函数式编程解决递归,会快很多。
9.filter返回为True，则保留该结果
10.sorted也是一个高阶函数，key参数指定传入的函数
11.{}.format，格式化输出。
12.使用递归函数需要防止栈溢出，函数调用是通过栈(stack)这种数据结构实现的，每当进入一个函数调用时，栈就会加一层栈帧。
13.解决递归调用栈溢出的方法是尾递归优化。
14.''的长度为0
